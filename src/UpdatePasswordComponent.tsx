import { View, Text } from 'react-native'
import React from 'react'

type password= string;

const UpdatePasswordComponent = ({navigation}:any) => {
  return (
    <View>
      <Text>UpdatePasswordComponent</Text>
    </View>
  )
}

export default UpdatePasswordComponent